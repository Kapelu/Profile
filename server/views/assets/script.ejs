<script>
	function altaDipExitosa() {alert('Distribuidor agregado con exito!')}
	const time = new Date()
	const style1 = [
		'color:grey',
		'font-family:system-ui',
		'font-size:15px',
		'text-shadow: 1px 2px greenbright',
	].join(';')
	const style2 = [
		'color:green',
		'font-family:system-ui',
		'font-size:15px',
		'text-shadow: 1px 2px greenbright',
	].join(';')
	const horaInicio =
		time.getHours() + ':' + time.getMinutes() + ':' + time.getSeconds()

	// Mensaje por consola

	console.log('%c# Server run on port: [ 5000 ]', style1)
	console.log('%cPerfect !!!', style2)
	console.log(`Hora del servidor => ${horaInicio}`)
</script>
